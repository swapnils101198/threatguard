# ---- Backend ----
PORT=8787
NODE_ENV=development

# Threat-intel API keys (backend only — NEVER ship in the extension)
GOOGLE_SAFE_BROWSING_API_KEY=
VIRUSTOTAL_API_KEY=
PHISHTANK_API_KEY=

# Cache
CACHE_TTL_SECONDS=3600
CACHE_MAX_ENTRIES=500